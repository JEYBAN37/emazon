export interface UserAux {
    name?:string | null,
    lastName?:string | null,
    dni?:string | null,
    telephone?:string | null,
    dateAge?:string | null,
    email?:string | null,
    password?:string | null,
}