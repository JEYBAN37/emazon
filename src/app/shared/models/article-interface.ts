export interface Article {
    name?: string | null,
    description?: string | null,
    quantity?: number | null, 
    price?: number | null,
    brand?: number | null,
    articleCategories?: number[] | null,
}

